#version 420 core

in vec4 color;
in vec2 UV;

out vec4 fragColor;

uniform sampler2D textureData;

void main()
{
    fragColor = texture(textureData, UV) * color;
    if (fragColor.a == 0.0f)
    {
        discard;
    }
}